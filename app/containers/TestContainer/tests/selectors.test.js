// import { fromJS } from 'immutable';
// import { makeSelectTestContainerDomain } from '../selectors';

// const selector = makeSelectTestContainerDomain();

describe('makeSelectTestContainerDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
